import styled from 'styled-components';

const ModalWrapper = styled.div``;

const ModalDialog = styled.dialog``;

const ModalContent = styled.div``;

export { ModalWrapper, ModalDialog, ModalContent };
